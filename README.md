# s3-zip-archiver-sam

Lambda (container image, deployed with AWS SAM) that zips every new JSON object
dropped into an S3 bucket, writes the archive back to the same bucket and removes
the original.

Work in progress.
